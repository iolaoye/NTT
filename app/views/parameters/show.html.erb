<% if notice != nil %>
  <div class="success_alert">
      <span class="closebtn" onclick="this.parentElement.style.display='none';">&times;</span>
      <%= notice %>
  </div>
<% end %>


<%= link_to t('general.edit'), edit_parameter_path(@parameter) %> |
<%= link_to t('general.back'), parameters_path %>
