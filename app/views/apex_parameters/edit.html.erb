<div class="page-header"><h1><%= t('general.editing') + " " + t('apex.parameter') %></h1></div>

<%= render 'form' %>

<%= link_to t('general.back'), apex_parameters_path, :class => "btn btn-primary" %>
