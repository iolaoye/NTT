<% if notice != nil %>
  <div class="success_alert">
      <span class="closebtn" onclick="this.parentElement.style.display='none';">&times;</span>
      <%= notice %>
  </div>
<% end %>


<%= link_to t('general.edit'), edit_layer_path %> |
<%= link_to t('general.back'), soil_layers_soil_path(session[:soil_id]) %>
