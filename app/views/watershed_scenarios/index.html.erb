<%= form_for :watershed, url: new_scenario_watershed_scenario_path do |f| %>
<table>
  <tr>
    <td><%= f.label :fields, 'Fields' %></td>
    <td><%= f.label :scenarios, 'Scenarios' %></td>
    <td></td>
  </tr>
  <tr>
    <td><%= collection_select :watershed, :field_id, Field.where(:location_id => session[:location_id]), :id, :field_name, {:prompt => 'Select Field'}, {:required => true} %></td>
    <td><%= collection_select :watershed, :scenario_id, @scenarios, :id, :name, {:prompt => 'SGIT PUSHelect Scenarios'}, {:required => true} %></td>
    <td class='action'><%= f.submit %></td>
  </tr>
</table>  
<% end %>
 
<h1><%= t('watershed_scenarios.watershed_scenarios_title')%></h1>
  
<p id="error_notice"><%= notice %></p>

<table class='gvTableStyle'>
  <tr class='gvHeaderStyle'>
    <th class="gvCellStyleTitle"><%= t('watershed.watershed_name')%></th>
    <th class="gvCellStyleTitle"><%= t('watershed.field_id')%></th>
    <th class="gvCellStyleTitle"><%= t('watershed.scenario_id')%></th>
    <th class="gvCellStyleTitle"><%= t('general.actions')%></th>
  </tr>

<% i=1 %>
<% @watershed_scenarios.each do |watershed_scenario| %>
<% if (i % 2  == 0) %>
   <tr class="gvRowStyleEven">
    <% else %>
   <tr class="gvRowStyleOdd">
    <% end %>
    <% i = i + 1 %>
  <tr class="gvRowStyle">
    <td class="gvCellStyleText"><%= Watershed.find_by_id(watershed_scenario.watershed_id).name %></td>
    <td class="gvCellStyleText"><%= Field.find_by_id(watershed_scenario.field_id).field_name %></td>
    <td class="gvCellStyleText"><%= Scenario.find_by_id(watershed_scenario.scenario_id).name %></td>
    <td class="gvCellStyleCheckbox">
      <%= link_to image_tag("delete.png"), watershed_scenario, title: t('general.delete'), method: :delete, data: { confirm: 'Are you sure?' } %>
    </td>
  </tr>
<% end %>
</table>

<br />
