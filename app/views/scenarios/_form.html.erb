<%= form_for(@scenario) do |f| %>
  <% if @scenario.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@scenario.errors.count, "error") %> prohibited this scenario from being saved:</h2>

      <ul>
      <% @scenario.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name, t('general.name') %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit t('scenario.create_scenario') %>
    <%= link_to t('general.back'), scenarios_path %>
  </div>
<% end %>
