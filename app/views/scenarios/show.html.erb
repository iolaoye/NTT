<% if notice != nil %>
  <div class="success_alert">
      <span class="closebtn" onclick="this.parentElement.style.display='none';">&times;</span>
      <%= notice %>
  </div>
<% end %>

<p>
  <b>Name:</b>
  <%= @scenario.name %>
  </p>

<%= link_to t('general.edit'), edit_scenario_path(@scenario) %> |
<%= link_to t('general.back'), field_scenarios_field_path(session[:field_id]) %>
