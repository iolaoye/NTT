<%= form_for(@bmplist) do |f| %>
  <% if @bmplist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bmplist.errors.count, "error") %> prohibited this bmplist from being saved:</h2>

      <ul>
      <% @bmplist.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
