<h1>Listing bmplists</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @bmplists.each do |bmplist| %>
  <tr>
    <td><%= bmplist.name %></td>
    <td><%= link_to t('general.show'), bmplist %></td>
    <td><%= link_to t('general.edit'), edit_bmplist_path(bmplist) %></td>
    <td><%= link_to 'Destroy', bmplist, method: :delete, data: { confirm: t('general.confirm') } %></td>
  </tr>
<% end %>
</table>

<br>

<%= link_to 'New Bmplist', new_bmplist_path %>
