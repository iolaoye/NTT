<%= form_for(@bmp3) do |f| %>
  <div class="field" id="depth" >
  </div>
  <div class="field" id = "depth" >
    <%= f.label :depth, t('bmp.depth') + " (" + t('bmp.ft') + ")" %>
    <%= f.number_field :depth, :step => 'any' %>
  </div>
<% end %>
