<%= form_for(@apex_control) do |f| %>
  <% if @apex_control.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@apex_control.errors.count, "error") %> prohibited this apex_control from being saved:</h2>

      <ul>
      <% @apex_control.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= "Code:" %>
    <%= f.label @control_code %>
  </div>
  <div class="field">
    <%= f.label :value %><br />
    <%= f.text_field :value %>
  </div>
  <div class="field">
    <%= "Low Range:" %>
    <%= @low_range %>
  </div>
  <div class="field">
    <%= "High Range:" %>
    <%=  @high_range %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
