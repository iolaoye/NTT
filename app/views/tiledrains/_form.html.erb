<%= form_with(model: tiledrain, local: true) do |form| %>
  <% if tiledrain.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tiledrain.errors.count, "error") %> prohibited this tiledrain from being saved:</h2>

      <ul>
      <% tiledrain.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
