<%= form_for(@watershed) do |f| %>
  <% if @watershed.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@watershed.errors.count, "error") %> prohibited this watershed from being saved:</h2>

      <ul>
      <% @watershed.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label t('general.name') %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit t('watershed.create_watershed') %>
    <%= link_to t('general.back'), list_watershed_path(session[:location_id]) %>
  </div>
<% end %>
