<h1>Listing fertilizer_types</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Spanish name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @fertilizer_types.each do |fertilizer_type| %>
  <tr>
    <td><%= fertilizer_type.name %></td>
    <td><%= fertilizer_type.spanish_name %></td>
    <td><%= link_to t('general.show'), fertilizer_type %></td>
    <td><%= link_to t('general.edit'), edit_fertilizer_type_path(fertilizer_type) %></td>
    <td><%= link_to 'Destroy', fertilizer_type, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Fertilizer type', new_fertilizer_type_path %>
